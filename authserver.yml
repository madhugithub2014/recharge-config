server:
  port: 9999
  contextPath: /uaa 

eureka:
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka.credentials.uri:http://localhost:8761}/eureka/

security:
  user:
    password: password